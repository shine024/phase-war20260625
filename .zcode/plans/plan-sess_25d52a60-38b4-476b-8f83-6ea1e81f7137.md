# 全面检查修复计划（C+H+M，共 15 项）

范围：CRITICAL(2) + HIGH(5) + MEDIUM(6) + 核实中发现的 2 个额外问题（fort_ 前缀失效、_guess_combat_kind 同步失效）。全部向后兼容。死代码（525 函数）本次不动。

---

## 🔴 CRITICAL

### C1. drops_inventory_panel @onready 路径缺失 ScrollContainer（崩溃）
**文件**：`scenes/ui/drops_inventory_panel.gd:12-15`
**改动**：4 个 @onready 路径补 `/ScrollContainer` 段：
- L12 `AllItems/ItemsGrid` → `AllItems/ScrollContainer/ItemsGrid`
- L13 `Materials/MaterialsGrid` → `Materials/ScrollContainer/MaterialsGrid`
- L14 `Blueprints/BlueprintsGrid` → `Blueprints/ScrollContainer/BlueprintsGrid`
- L15 `Lore/LoreGrid` → `Lore/ScrollContainer/LoreGrid`
**验证**：tscn 节点路径已核实（91/97/107/113/123/129/139/145 行）全含 ScrollContainer。

### C2. 改造模块 _CARD_PREFIXES 旧前缀全部失效 + fort_ 前缀死匹配
**根因**：v7.x 卡 ID 规范化（提交 c8d6cc4）后前缀表未同步。`fort_` 前缀匹配 0 张卡（堡垒卡 ID 是 `ww1_fort_*` 中缀形式）。
**文件与改动**（8 个数据文件 + 1 个推断函数）：

1. **`armor_mods.gd:420`** — 更新 `_CARD_PREFIXES` 覆盖全部装甲卡新 ID；`_matches_card`(L414) 加 `cold_m14` 特例排除（`cold_m1` 前缀会误匹配 `cold_m14` 步兵）
2. **`air_mods.gd:196`** — `fut_scout_drone` → `mod_inf_scout_drone`，补 `fut_nano_drone`
3. **`anti_air_mods.gd:176`** — `cold_zsu23` → `cold_sup_zsu23`，`mod_m6` → `mod_sup_m6`
4. **`artillery_mods.gd:259`** — 保持原范围（仅纯炮兵/机枪），更新失效前缀：`ww1_m81`→`ww1_arty_m81`、`ww1_77mm`→`ww1_arty_77mm`、`ww1_engineer`→`ww1_sup_engineer`、`ww2_m81`→`ww2_arty_m81`、`cold_m113`→`cold_sup_m113`、`mod_m270`→`mod_arty_m270`
5. **`engineer_mods.gd:148`** — `ww1_engineer`→`ww1_sup_engineer`；删除死链 `cold_avlb`/`mod_m9ace`（`engineer_evolution.gd:5` 注释已自证）；保留 `fut_nano_drone`
6. **`infantry_mods.gd:590`** — `ww2_panzerschrek`→`ww2_inf_panzerschrek`、`ww2_bazooka`→`ww2_inf_bazooka`、`mod_technical`→`mod_inf_technical`、`fut_scout_mech`→`fut_inf_scout_mech`（保持不含侦察子集的原设计）
7. **`recon_mods.gd:172`** — `ww1_cavalry`→`ww1_inf_cavalry`、`fut_scout_mech`→`fut_inf_scout_mech`、`fut_scout_drone`→`mod_inf_scout_drone`
8. **`fort_mods.gd:150`** — `"fort_"` 单前缀 → 列出全部 10 张堡垒卡完整 ID
9. **`data/modification_modules/__init__.gd:84-105`** — `_guess_combat_kind` 同步修复同款失效前缀（通用改造兵种判定依赖它）

**兜底说明**：`get_for_unit_type(card.combat_kind)` 兜底让改造仍可安装，此修复恢复 UI 精筛标灰准确性。

---

## 🟠 HIGH

### H1. 3 个信号 emit 无订阅者 → 接 toast
**文件**：`scenes/ui/bottom_instrument_bar.gd`（部署入口，最合适的监听位置）
**改动**：_ready 中 connect 3 个信号：
- `energy_insufficient` → toast "能量不足，无法部署"（红/警告色）
- `phase_field_level_up` → toast "相位场提升至 Lv X！获得 Y 点"（正面提示）
- `phase_field_xp_changed` → 不接 toast（频繁触发会刷屏），仅确保信号链路完整（留作未来 UI hook）

**实现**：用 `SignalBus.has_signal` 守卫 + 现有 toast 系统（`SignalBus.show_toast`，v6.6 已修复 ToastManager 连接）。

### H2. synthesis_panel / enemy_origin_mod_panel 数据源补 InstanceRegistry 全集（铁律 2）
**文件**：
- `scenes/ui/synthesis_panel.gd:217-226` — 在 BlueprintManager + SaveManager 队列之外，增加 `InstanceRegistry.get_all_instance_ids()` → 归一化到 base card_id → 补入候选
- `scenes/ui/enemy_origin_mod_panel.gd:197-212` — 同上（EOM 按 base card_id 操作，补 InstanceRegistry base card_id 全集去重）

**参考实现**：`modification_panel._refresh_card_list` / `growth_panel._load_unlocked_cards`（已合规的三层数据源写法）。

### H3. InstanceRegistry 的 EOM 死字段与 BlueprintManager 实际存储不一致
**经核实**：实际 EOM 装备走 `BlueprintManager.blueprint_enemy_origin_mod`（card_id key，L80/204）；`InstanceRegistry._enemy_origin_mod`（instance_id key）从未被 equip_eom 写入，仅 `card_evolution_manager` 进化迁移读它（永远读空串）。是预留死字段，非破坏性 bug。
**处理**（保守，不删字段）：`instance_registry.gd:40-41` 加注释说明"当前 EOM 装备实际走 BlueprintManager.card_id 字典，本字段为预留/未接入，进化迁移读到的恒为空"。同步修正 AGENTS.md 相关描述（若提及）。

### H4. 删除游离旧副本 `scenes/ui/leaderboard_panel.gd`
**核实**：0 引用（tscn 绑定的是 `scenes/ui/leaderboard/leaderboard_panel.gd` 子目录版）。48KB 死文件，删除消除维护混乱。
**注意**：工作区已有该文件的未提交改动（git status 显示 M），删除前会先确认这些改动无价值（属旧副本，未被任何场景引用）。

### H5. instance_registry._load_one_instance 找不到模板只 push_warning
**文件**：`managers/instance_registry.gd:261`
**改动**：`push_warning` → `push_error`，并在 return 前记录失败 instance_id 到内存（供调试）。养成数据丢失时至少有 error 日志。

---

## 🟡 MEDIUM

### M1. quest_manager._set_progress 字典引用语义 bug
**文件**：`managers/quest_manager.gd:225-229`
**改动**：`_set_progress` 开头加守卫——`if not _accepted.has(qid): return`（未接受任务不记录进度，语义正确）。避免 `get(qid,{})` 绑定临时空字典导致写入丢失。

### M2. company_store.gd 无 GD 兜底数据
**文件**：`data/company_store.gd:27`
**改动**：`LEGACY_ITEMS = []` 保持空但加注释说明设计（JSON 是唯一源，加载失败时商店空是预期行为）。**不补全量兜底数据**（50 条商品手填成本高、且 JSON 加载失败极罕见），仅文档化现状避免误判为 bug。

### M3. 删除未被加载的陈旧聚合 JSON
**文件**：`data/json/enemy_phase_equipment.json`
**核实**：`enemy_phase_equipment.gd:14-17` 实际加载 4 个独立 JSON（instruments/platforms/weapons/energy_cards），不读此聚合文件。删除消除漂移风险。

### M4. save_migration 删旧存档无备份
**文件**：`scripts/systems/save_migration.gd:108`
**改动**：`DirAccess.remove_absolute` 前先复制到 `.bak`（若存在），迁移成功后删 bak，失败可回滚。

### M5. 12 处活跃裸 print 加 DEBUG 守卫
**文件**：`managers/ui_lazy_loader.gd:246,249,258,261`、`scenes/main.gd:322,326,356,370,626`、`scenes/battlefield/battlefield.gd:82`、`scenes/ui/backpack_panel.gd:1037`
**改动**：包裹 `if OS.is_debug_build(): print(...)` 或 `if GameConfig.debug_verbose:`（看项目现有模式）。非调试构建静默。

### M6. 4 处 TODO + DEPRECATED 空回调
**处理**：本项改为**仅文档化**（不改代码）——这些是已知的待实现/已迁移遗留，AGENTS.md 已有记录，强行清理可能影响后续。在汇总里列出位置即可，不实施改动。

---

## 验证方式
1. **Godot headless --check-only**：确认无语法错误（注：项目体量 5 分钟超时属既有现象，启动到 DefaultCards 构建无错即视为通过）
2. **Grep 静态核对**：C2 的 8 个前缀表逐一 `begins_with` 验证匹配范围正确（含 cold_m14 排除、fort 10 卡全覆盖、无误伤）
3. **改造匹配冒烟测试**：写 `tests/mod_prefix_smoke.gd`（SceneTree 模式，对每张卡验证 get_mods_for_card 返回的精筛结果符合 combat_kind）

## 不做的事
- 死代码（525 函数 / 12 个未接入基础设施类）— 用户明确本次不动
- M6 TODO/DEPRECATED — 仅文档化
- 完整运行游戏实机验证 — 部分修复（如信号 toast、面板数据源）需游戏内验证，本轮以静态+headless 验证为准，标注需实机验证项