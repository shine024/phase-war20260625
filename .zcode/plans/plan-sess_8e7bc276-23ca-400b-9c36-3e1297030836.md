# 相位师 Boss 关大地图标记 + 第 49 关驻守化

## 决策确认
- **Boss 标记方式**：金色字体 + font_outline 描边（绕开 StyleBoxFlat 单一 border_color 限制，与剧情★/占领边框零冲突）
- **第 49 关**：固定为 `enemy_master_014`（雷霆钢铁·维克多 Lv19），填补 45关(Lv18)→49→50关(Lv20) 梯度

## 改动 1：`data/phase_master_garrison.gd` — 第 49 关并入驻守表
在 `GARRISON_LEVEL_TO_MASTER` 字典的 COLD 时代区段（45 与 50 之间）插入：
```gdscript
	49: "enemy_master_014",  # 雷霆钢铁·维克多 Lv19 thunder_steel（原第49关硬编码，驻守化）
```
- 相位师 014 已在 `enemy_phase_masters_cold.gd` 定义，`get_master_by_id` 能正常返回，`_build_garrison_config` 可正常构建完整装备配置。
- 注释顶部 "19个关卡" 同步改为 "20个关卡"。

## 改动 2：`managers/game_manager.gd` — 移除 force_phase_master_battle 硬编码
`check_phase_master_encounter`（72-175行）中：
- **删除** 83行 `var force_phase_master_battle = (current_level == 49)`
- **删除** 84行 `if not force_phase_master_battle:` 包裹层，把 85-101行（新手保护期 + 递增保底概率门）**降为无条件执行**（去掉一层缩进）
- **删除** 82行注释、102-104行 `if force_phase_master_battle: DEBUG pass` 块
- 驻守检查（74-81行）先于概率门执行，第 49 关会在驻守分支提前返回，行为等价且更统一。
- 更新函数文档注释（68-71行）：注明"驻守关(20个)100%优先 → 非驻守关走概率门"。

## 改动 3：`scenes/world_map.gd` — Boss 关按钮金色标记
在文件顶部 preload 区（37行后）加：
```gdscript
const PhaseMasterGarrison = preload("res://data/phase_master_garrison.gd")
const EnemyPhaseMasters = preload("res://data/enemy_phase_masters.gd")
```
（同时让 `_collect_level_info` 1012/1015行的局部 preload 复用这两个顶部 const，减少重复。）

`_make_level_button`（341-425行）改造：
- **346行后**新增：查 `PhaseMasterGarrison.is_garrison_level(level_index)`，若是则查相位师名+等级，供 tooltip 用。
- **字体样式**（373/381行附近）：Boss 关覆盖 `font_color` 为金色 `Color(1.0, 0.84, 0.3, 1.0)`，并加 `font_outline_color = Color(1.0, 0.65, 0.2, 0.9)` + `font_outline_size = 2`（Godot 4.5 Button 支持）。当前关+Boss 重叠时保留当前关发光边框，字体仍金色描边。
- **tooltip**：Boss 关追加 `⚔ 相位师首领：<名> Lv.<等级>`（与剧情★、占领 tooltip 共存，append 模式）。

复用 `_collect_level_info` 现有的 `garrison_master_name` 采集模式（1010-1018行），不在按钮渲染时重复格式化逻辑。

## 改动 4（附带清理）：`resources/game_config.gd` — 删除 phase_master_boss_level 死配置
`phase_master_boss_level: int = 49`（22/57/119行三处）从未被任何 .gd 引用（game_manager 用字面量 49，现已驻守化）。删除这3处避免文档与代码不一致。

## 不改动
- `_show_level_info_popup`（584-587行驻守相位师行）：第 49 关驻守化后会自动显示（之前 49 关走随机路径，弹窗无此行；现在会显示"驻守相位师：雷霆钢铁·维克多 Lv.19"）。无需改弹窗代码。
- 随机相位师机制、其他 19 个驻守关、相位师数据本身。

## 验证
1. `godot --headless --check-only`（语法 + autoload 链路）
2. Grep 静态核对：
   - `force_phase_master_battle` 在 game_manager.gd 0 残留
   - `enemy_master_014` 在 garrison 字典 + `get_master_by_id` 可查
   - `is_garrison_level` 在 world_map._make_level_button 调用正确
   - `phase_master_boss_level` 在全项目 0 残留
3. 实机验证（需用户运行）：大地图 Boss 关金色字体描边 + tooltip，第 49 关弹窗显示维克多 Lv19。

## 改动文件清单（4 个）
- `data/phase_master_garrison.gd`（+1 驻守条目 +注释计数）
- `managers/game_manager.gd`（移除 force 硬编码，概率门降级）
- `scenes/world_map.gd`（顶部 const + Boss 金色标记 + tooltip）
- `resources/game_config.gd`（删 3 处死配置）