# 情报面板模式区分计划

## 目标
在**背包中打开**情报面板时，显示**最新/完整**的情报数据（含 BPManager 养成数据 + 战场实时数据）；
在**相位仪**和**战场**中打开时，保持**原先的简化显示**（不加载 BPManager 养成数据）。

---

## 当前架构

### 三个入口调用同一个 `CardInfoPanel`，通过 `_current_mode` 区分：

| 入口 | 文件 | 调用方法 | 模式 |
|------|------|---------|------|
| 背包 | `backpack_panel.gd` | `show_card_info(card)` | `MODE_BACKPACK = 0` |
| 相位仪 | `phase_instrument_panel.gd` | `show_card_info(card)` | `MODE_PHASE_INSTRUMENT = 1` |
| 战场 | `unit_info_panel.gd` | `show_unit_info(unit, is_player)` | `MODE_BATTLEFIELD = 2` |

### 情报数据刷新入口
`card_info_panel.gd` → `_refresh_info_sections(card)` 负责刷新情报 Tab 所有内容：
- `summary_label` — 三维攻防（来自 `BackpackCombatPreview.build_line`）
- `affix_label` — 词条（来自 `_build_card_affix_summary`）
- `star_label` — 星级强化（来自 `_build_star_lines`）
- `law_label` — 法则影响（战场单位才有）
- `nurture_label` — 养成摘要（军衔/战力/等级/进化/改造）
- `desc_label` — 描述
- `flavor_label` — 风味文本

### 关键差异
- **背包模式**：需要加载 `BlueprintManager` 获取养成数据（星级/等级/军衔/战力）
- **相位仪/战场模式**：不需要 BlueprintManager，显示简化版

---

## 修改计划

### 文件 1: `scenes/ui/card_info_panel.gd`

#### 修改点 1: `_refresh_info_sections(card)` — 模式分支

**位置**: 第 357 行 `func _refresh_info_sections(card: CardResource) -> void`

**修改前**:
```gdscript
func _refresh_info_sections(card: CardResource) -> void:
    if card == null:
        return
    # 三维攻防
    if summary_label:
        var preview: String = BackpackCombatPreview.build_line(card)
        if preview.begins_with("战斗中："):
            preview = preview.substr(5)
        summary_label.text = preview if not preview.is_empty() else card.summary_line
    # 词条
    if affix_label:
        affix_label.text = _build_card_affix_summary(card) if card.card_type == GC.CardType.COMBAT_UNIT else ""
    # 星级强化
    if star_label:
        star_label.text = _build_star_lines(card)
    # 法则影响
    if law_label:
        law_label.text = ""  # 卡牌模式无法则影响行（战场单位才会有）
    # 养成摘要
    if nurture_label:
        nurture_label.text = _build_nurture_text(card)
    # 描述
    if desc_label:
        desc_label.text = card.description
    # 风味
    if flavor_label:
        flavor_label.text = card.flavor_text
    # 隐藏战场专用状态区
    if status_section:
        status_section.visible = false
```

**修改后**:
```gdscript
func _refresh_info_sections(card: CardResource) -> void:
    if card == null:
        return
    # ── 背包模式：加载完整养成数据 ──
    # 相位仪/战场模式：保持简化显示，不调用 BlueprintManager
    var use_full_data: bool = (_current_mode == PanelMode.MODE_BACKPACK)
    
    # 三维攻防
    if summary_label:
        var preview: String = BackpackCombatPreview.build_line(card)
        if preview.begins_with("战斗中："):
            preview = preview.substr(5)
        summary_label.text = preview if not preview.is_empty() else card.summary_line
    
    # 词条：仅背包模式加载完整词条数据
    if affix_label:
        if use_full_data and card.card_type == GC.CardType.COMBAT_UNIT:
            affix_label.text = _build_card_affix_summary(card)
        else:
            affix_label.text = ""
    
    # 星级强化：仅背包模式
    if star_label:
        if use_full_data:
            star_label.text = _build_star_lines(card)
        else:
            star_label.text = ""
    
    # 法则影响
    if law_label:
        law_label.text = ""  # 卡牌模式无法则影响行（战场单位才会有）
    
    # 养成摘要：仅背包模式
    if nurture_label:
        if use_full_data:
            nurture_label.text = _build_nurture_text(card)
        else:
            nurture_label.text = ""
    
    # 描述
    if desc_label:
        desc_label.text = card.description
    
    # 风味
    if flavor_label:
        flavor_label.text = card.flavor_text
    
    # 隐藏战场专用状态区
    if status_section:
        status_section.visible = false
```

#### 修改点 2: `_build_card_affix_summary(card)` — 模式分支

**位置**: 第 388 行 `func _build_card_affix_summary(card: CardResource) -> String`

**修改前**:
```gdscript
func _build_card_affix_summary(card: CardResource) -> String:
    if card.card_type != GC.CardType.COMBAT_UNIT:
        return ""
    var tree := Engine.get_main_loop() as SceneTree
    if tree == null or tree.root == null:
        return ""
    var root: Node = tree.root
    var mll: Node = root.get_node_or_null("ManagerLazyLoader")
    if mll and mll.has_method("ensure_loaded"):
        mll.ensure_loaded("affix")
    var bm: Node = root.get_node_or_null("BlueprintManager")
    var am: Node = root.get_node_or_null("AffixManager")
    var era: int = 0
    var gm: Node = root.get_node_or_null("GameManager")
    if gm and "current_level" in gm:
        era = GC.get_era_for_level(int(gm.current_level))
    if card.card_type == GC.CardType.COMBAT_UNIT:
        var stats: UnitStats = UnitStatsTable.build_stats_from_card(card, era)
        if bm and bm.has_method("apply_growth_to_stats"):
            bm.apply_growth_to_stats(stats, card, [])
        if am and am.has_method("apply_affixes_to_stats"):
            am.apply_affixes_to_stats(stats, card, [])
        return _build_affix_summary_lines(stats)
    return ""
```

**修改后**:
```gdscript
func _build_card_affix_summary(card: CardResource) -> String:
    if card.card_type != GC.CardType.COMBAT_UNIT:
        return ""
    # 非背包模式：不加载 BPManager/AffixManager，返回空
    if _current_mode != PanelMode.MODE_BACKPACK:
        return ""
    var tree := Engine.get_main_loop() as SceneTree
    if tree == null or tree.root == null:
        return ""
    var root: Node = tree.root
    var mll: Node = root.get_node_or_null("ManagerLazyLoader")
    if mll and mll.has_method("ensure_loaded"):
        mll.ensure_loaded("affix")
    var bm: Node = root.get_node_or_null("BlueprintManager")
    var am: Node = root.get_node_or_null("AffixManager")
    var era: int = 0
    var gm: Node = root.get_node_or_null("GameManager")
    if gm and "current_level" in gm:
        era = GC.get_era_for_level(int(gm.current_level))
    if card.card_type == GC.CardType.COMBAT_UNIT:
        var stats: UnitStats = UnitStatsTable.build_stats_from_card(card, era)
        if bm and bm.has_method("apply_growth_to_stats"):
            bm.apply_growth_to_stats(stats, card, [])
        if am and am.has_method("apply_affixes_to_stats"):
            am.apply_affixes_to_stats(stats, card, [])
        return _build_affix_summary_lines(stats)
    return ""
```

#### 修改点 3: `_build_star_lines(card)` — 模式分支

**位置**: 第 415 行 `func _build_star_lines(card: CardResource) -> String`

**修改前**:
```gdscript
func _build_star_lines(card: CardResource) -> String:
    if BlueprintManager == null or not BlueprintManager.has_method("get_star_enhancement_lines"):
        return ""
    var detail_star: int = int(card.enhance_level)
    var lines: Array[String] = BlueprintManager.get_star_enhancement_lines(card.card_id, detail_star)
    if lines.is_empty():
        return "★%d（无加成）" % detail_star
    return "★%d\n- %s" % [detail_star, "\n- ".join(lines)]
```

**修改后**:
```gdscript
func _build_star_lines(card: CardResource) -> String:
    # 非背包模式：不加载 BPManager
    if _current_mode != PanelMode.MODE_BACKPACK:
        return ""
    if BlueprintManager == null or not BlueprintManager.has_method("get_star_enhancement_lines"):
        return ""
    var detail_star: int = int(card.enhance_level)
    var lines: Array[String] = BlueprintManager.get_star_enhancement_lines(card.card_id, detail_star)
    if lines.is_empty():
        return "★%d（无加成）" % detail_star
    return "★%d\n- %s" % [detail_star, "\n- ".join(lines)]
```

#### 修改点 4: `_build_nurture_text(card)` — 模式分支

**位置**: 第 424 行 `func _build_nurture_text(card: CardResource) -> String`

**修改前**:
```gdscript
func _build_nurture_text(card: CardResource) -> String:
    if card == null or BlueprintManager == null:
        return ""
    var parts: Array[String] = []
    if card.card_type == GC.CardType.COMBAT_UNIT:
        var rank_info: Dictionary = card.get_military_rank() if card.has_method("get_military_rank") else {}
        var rank_name: String = str(rank_info.get("name", ""))
        if not rank_name.is_empty():
            parts.append("军衔：%s" % rank_name)
        var power: int = card.get_current_power() if card.has_method("get_current_power") else 0
        parts.append("战力：%d" % power)
    if BlueprintManager.has_method("get_card_xp_progress"):
        var prog: Dictionary = BlueprintManager.get_card_xp_progress(card.card_id)
        var lvl: int = int(prog.get("level", 1))
        var lv_text: String = "Lv.%d" % lvl
        if BlueprintManager.has_method("get_card_breakthroughs"):
            var bt: int = BlueprintManager.get_card_breakthroughs(card.card_id)
            if bt > 0:
                lv_text += " (突破 %d)" % bt
        parts.append(lv_text)
    if "evolution_stage" in card and str(card.evolution_stage) != "":
        var stage: String = str(card.evolution_stage)
        if not stage.is_empty():
            parts.append("进化 %s" % stage)
    if card.card_type == GC.CardType.COMBAT_UNIT and "mods" in card:
        parts.append("改造 %d/9" % card.mods.size())
    if not parts.is_empty():
        return " · ".join(parts)
    return ""
```

**修改后**:
```gdscript
func _build_nurture_text(card: CardResource) -> String:
    # 非背包模式：不加载 BPManager
    if _current_mode != PanelMode.MODE_BACKPACK:
        return ""
    if card == null or BlueprintManager == null:
        return ""
    var parts: Array[String] = []
    if card.card_type == GC.CardType.COMBAT_UNIT:
        var rank_info: Dictionary = card.get_military_rank() if card.has_method("get_military_rank") else {}
        var rank_name: String = str(rank_info.get("name", ""))
        if not rank_name.is_empty():
            parts.append("军衔：%s" % rank_name)
        var power: int = card.get_current_power() if card.has_method("get_current_power") else 0
        parts.append("战力：%d" % power)
    if BlueprintManager.has_method("get_card_xp_progress"):
        var prog: Dictionary = BlueprintManager.get_card_xp_progress(card.card_id)
        var lvl: int = int(prog.get("level", 1))
        var lv_text: String = "Lv.%d" % lvl
        if BlueprintManager.has_method("get_card_breakthroughs"):
            var bt: int = BlueprintManager.get_card_breakthroughs(card.card_id)
            if bt > 0:
                lv_text += " (突破 %d)" % bt
        parts.append(lv_text)
    if "evolution_stage" in card and str(card.evolution_stage) != "":
        var stage: String = str(card.evolution_stage)
        if not stage.is_empty():
            parts.append("进化 %s" % stage)
    if card.card_type == GC.CardType.COMBAT_UNIT and "mods" in card:
        parts.append("改造 %d/9" % card.mods.size())
    if not parts.is_empty():
        return " · ".join(parts)
    return ""
```

---

## 参数汇总

| 参数 | 值 | 说明 |
|------|-----|------|
| `PanelMode.MODE_BACKPACK` | `0` | 背包模式 — **加载完整数据** |
| `PanelMode.MODE_PHASE_INSTRUMENT` | `1` | 相位仪模式 — **简化显示** |
| `PanelMode.MODE_BATTLEFIELD` | `2` | 战场模式 — **简化显示** |
| `_current_mode` | 运行时设置 | 由入口决定 |
| `use_full_data` | `(_current_mode == PanelMode.MODE_BACKPACK)` | 局部变量 |

---

## 各模式下显示内容对照表

| 字段 | 背包模式 (0) | 相位仪模式 (1) | 战场模式 (2) |
|------|-------------|---------------|-------------|
| 三维攻防 (summary) | ✅ 完整 | ✅ 完整 | ✅ 完整 |
| 词条 (affix) | ✅ BPManager+AffixManager | ❌ 空 | ❌ 空 |
| 星级强化 (star) | ✅ BPManager | ❌ 空 | ❌ 空 |
| 法则影响 (law) | ❌ 空 | ❌ 空 | ❌ 空 |
| 养成摘要 (nurture) | ✅ BPManager | ❌ 空 | ❌ 空 |
| 描述 (desc) | ✅ card.description | ✅ card.description | ✅ card.description |
| 风味 (flavor) | ✅ card.flavor_text | ✅ card.flavor_text | ✅ card.flavor_text |

---

## 不变的部分（不修改）

- `show_card_info(card, at_position)` — 入口方法不变
- `show_unit_info(unit, is_player, at_position)` — 入口方法不变
- `set_panel_mode(mode, context_data)` — 模式设置不变
- `hide_panel()` — 隐藏不变
- 三个入口文件（`backpack_panel.gd`, `phase_instrument_panel.gd`, `unit_info_panel.gd`）— 不改

---

## 验证步骤

1. 打开背包 → 点击卡牌 → 情报 Tab 应显示完整数据（星级/等级/军衔/战力/词条）
2. 打开相位仪 → 点击卡牌 → 情报 Tab 应只显示攻防+描述+风味
3. 进入战场 → 点击单位 → 情报 Tab 应只显示攻防+描述+风味
4. 切换不同卡牌 → 数据正确刷新
