# 

# 晨昏大陆 · 黑日战线（方案 11，定稿）生图清单 · 2026-08-28

> 布局已切换为方案 11（`world_map.gd` 顶部 `MAP_SCHEME = 11`；6/8 布局代码保留可切回）。
> 现为占位实现：底图=黑海+代码五档天光色带、家=灯塔图兜底、黑日=代码绘制。
> 本节 = 方案 11 **新增**资产（4 张）+ 资源贴合判断。画风/色板/总则与主清单一致。

## 资源贴合判断（复用 vs 重生成）

| 现有资产                                                                        | 判定      | 说明                                 |
| --------------------------------------------------------------------------- | ------- | ---------------------------------- |
| `bubble_era_*.png` ×5、`bubble_cleared`、`bubble_boss`                        | **复用**  | 相位泡悬在战线上空，与"门在吞光"设定自洽              |
| `gate_near.png`（整幅）                                                         | **复用**  | 第三幕"巨门占满东方天际"的终局近接演出，首次有真正用武之地     |
| `map_void_base.png`、`gate_far/mid`、`lighthouse_fortress`、`debris_1-8`（太空残骸） | **不贴合** | 均为太空语境，方案 11 是行星地表；留作方案 6/8 备份不删   |
| 基地重设计的余烬要塞地表件                                                               | **不贴合** | 正交侧视剖面视角，与俯瞰大陆图视角不匹配 → 新画俯视/斜视掩体入口 |

## 新增资产（4 张）

| #   | 文件名                          | 构图       | 用途                                 |
| --- | ---------------------------- | -------- | ---------------------------------- |
| B1  | `dawn_dusk_continent.png`    | 16:9 整幅  | 晨昏大陆长卷底图（核心资产，替换黑海+色带占位）           |
| B2  | `mountain_bunker_marker.png` | 3:4 白底精灵 | 山脉掩体入口（家），替换灯塔占位                   |
| B3  | `black_sun.png`              | 1:1 白底精灵 | 黑日（50-89 关）；缩到 ~18px 即第一幕"暗星"，一图两档 |
| B4  | `wreck_sheet_land.png`       | 1:1 九宫格  | 陆战残骸 ×8（替换太空残骸装饰），后处理切片            |

## 逐资产 Prompt

### B1. 晨昏大陆底图 `dawn_dusk_continent.png`（16:9，1920 档，部署放大 2560×1440）

```
[P_BG]
A vast hand-painted war continent seen from high above at a slight angle,
the land sweeping horizontally across the whole frame from a golden eternally-lit west
to a pitch-black polar night east,
left fifth: golden lit trench plains with zigzag trench lines and shell craters,
second fifth: autumn forests with broken battle roads,
middle fifth: grey-blue snowy plains with radar towers and wire fences,
fourth fifth: half-demolished dense city blocks in deep dusk,
right fifth: scorched blackened glassland with faint ember cracks in near total darkness,
the lighting gradually dims from west to east across the entire land with no hard edge,
on the far eastern horizon floats one tiny dim black star with a whisper of pale cyan halo,
no characters, no text, somber muted palette, This War of Mine grit
```

验收：五段地貌横向带状可辨；光照渐变平滑无硬接缝；东端暗星必须极小（彩蛋级）；无文字无人物。

### B2. 山脉掩体入口 `mountain_bunker_marker.png`（3:4，1024 档）

```
[P_SPR]
A bunker entrance built into a rugged mountain slope, hand-painted,
heavy concrete blast doors embedded in dark rock,
small warm amber windows #ffb45e glowing softly on the mountainside,
a small pale cyan lamp #00e5ff glowing above the door frame,
rust-orange #8a4b2f steel reinforcements, sandbags and one thin antenna,
seen from a slightly elevated angle matching a high-angle continent map,
the only warm home in a darkening land, lonely but defiant
```

验收：暖窗为唯一暖光源；斜视角（能放进俯瞰大陆图）；门上方青灯（相位标记）；粗粝感与余烬要塞同款。

### B3. 黑日 `black_sun.png`（1:1，768-1024 档）

```
[P_SPR]
A single black sun disc floating in empty sky, hand-painted,
a perfect circle of absolute darkness with thin pale cyan #00e5ff fracture veins
across its surface, a faint dim corona halo tightly around the disc,
ominous, quiet, wrong — a star that emits no light
```

验收：正圆、黑体纯黑、青裂纹细而清晰、外晕极淡不溢出；缩到 16px 仍可辨黑点（第一幕暗星态）。
部署后：1-49 关 scale≈18px（暗星）/ 50-89 关 ≈240px（黑日）/ 90+ 关 ≈420px 压东天（代码已按此接好，图到位自动切换）。

### B4. 陆战残骸表 `wreck_sheet_land.png`（1:1 九宫格，后处理 PIL 切 8 张）

```
[P_SPR]
A loose grid of 8 small separate land-battlefield debris objects on white background,
each fully inside its own cell with wide margins:
a bent trench wooden post with barbed wire coil, a burnt-out tank hull,
a crashed airplane wing, a broken artillery gun barrel,
a rusted supply truck, a collapsed stone bridge segment,
a shell crater with scorched rim, a leaning broken flagpole with torn flag,
hand-painted muted colors, rust and ash tones
```

## 部署

- B1：整幅转存，LANCZOS 放大到 2560×1440 替换 `map_void_base` 的用法（方案 11 分支），并**移除**构建里的五档天光 ColorRect 占位与西端暖带（底图自带渐变）。
- B2/B3：白底转透明 + 裁边；`deploy_world_map_assets.py` 的 `BASES`/切片表无需改（走精灵流程）。
- B4：加 `SLICES` 条目 `("wreck_sheet_land", (4, 2))`，切片前缀改 `wreck`，散布代码改用 `wreck_%d`。
- 方案 6/8 弃用的太空资产**不删**（切回布局仍要用），但下一次美术备份 zip 需含新增 4 张。
