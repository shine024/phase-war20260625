# 侦查暴击标注系统（Crit Mark）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为侦查类单位新增「暴击标注」改造（`rec_14_crit_designator`）：攻击命中概率给目标挂暴击标注 → 全队远程优先集火该目标 → 攻击该目标暴击率 +50%，持续 5 秒，独立于现有易伤 mark 系统。

**Architecture:** 复用现有 `_apply_mark` 的 meta 挂载范式与 `bullet.gd` 暴击 roll 链路，新增独立的 `_crit_marked_until` / `_crit_mark_bonus` meta + `crit_mark_*` UnitStats 字段。四条链路：施加（`module_effect_handler`）→ 暴击触发（`bullet.gd`）→ 过期清理（三处 `take_damage`）→ 索敌优先（`target_selection`）。不改动 `rec_13`/`aa_13`/激光/反击炮。

**Tech Stack:** Godot 4.5 / GDScript / 纯静态类数据层 / SceneTree smoke test

**Spec:** `docs/superpowers/specs/2026-07-14-crit-mark-design.md`

---

## ⚠️ 全局约定

- **Commit 授权**：本计划每个 Task 末尾含 `git commit` 步骤，但项目 `CLAUDE.md` 规定「No commits without user instruction」。**执行 commit 前必须获得用户明确指示**；未获指示时仅执行 `git add` 暂存并停下询问。
- **Godot 命令**：可执行路径 `/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe`，**不加** `--rendering-driver opengl3`（会导致 headless 卡死）。全量 `--check-only` >70s，smoke test 约 10-20s。
- **行号为撰写时快照**，Edit 时以实际文件内容为准（用唯一上下文匹配）。

## File Structure

| 文件 | 责任 | 改动 |
|---|---|---|
| `resources/unit_stats.gd` | 单位属性 Resource | +3 字段 |
| `resources/unit_stats_table.gd` | 改造 effects → UnitStats 贯通 | 读取段 + 写回段 |
| `scripts/systems/modification_registry.gd` | effect key → 字段映射 | +3 分支 |
| `data/modification_modules/recon_mods.gd` | recon 改造数据 | +`rec_14` |
| `scripts/battle/module_effect_handler.gd` | 命中副作用施加 | +`_apply_crit_mark` + 调用 |
| `scenes/units/bullet.gd` | 暴击 roll | +crit_mark 加成 |
| `scripts/battle/target_selection.gd` | 索敌 | +`_prioritize_crit_marked` + const |
| `scenes/units/construct_unit.gd` | 玩家单位 take_damage | +惰性清理 |
| `scenes/units/enemy_unit.gd` | 敌方单位 take_damage | +惰性清理 |
| `scenes/units/swarm_enemy_slot.gd` | 蜂群槽 take_damage | +惰性清理 |
| `tests/crit_mark_smoke.gd` | smoke test | 新建 |

---

## Task 1: UnitStats 字段层 + 贯通

**Files:**
- Modify: `resources/unit_stats.gd:235`（mark 字段块后）
- Modify: `resources/unit_stats_table.gd:366`（读取段）、`:435`（写回段）
- Create: `tests/crit_mark_smoke.gd`

- [ ] **Step 1: 写失败测试（新建 smoke test 含 test 1）**

创建 `tests/crit_mark_smoke.gd`：

```gdscript
extends SceneTree
## 暴击标注系统（Crit Mark）smoke test
## 运行：/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/crit_mark_smoke.gd"

const UnitStats = preload("res://resources/unit_stats.gd")
const ReconMods = preload("res://data/modification_modules/recon_mods.gd")
const ModRegistry = preload("res://scripts/systems/modification_registry.gd")

func _init():
	print("═══════════════════════════════════════════")
	print("  暴击标注系统 (Crit Mark) Smoke Test")
	print("═══════════════════════════════════════════")
	var all_pass: bool = true
	all_pass = _test_unit_stats_fields() and all_pass
	print("\n═══════════════════════════════════════════")
	if all_pass:
		print("  ✅ 全部 PASS")
	else:
		print("  ❌ 存在失败项")
	print("═══════════════════════════════════════════")
	quit(0 if all_pass else 1)

## 测试 1：UnitStats 暴击标注字段存在且默认值正确
func _test_unit_stats_fields() -> bool:
	print("\n[测试 1] UnitStats crit_mark 字段默认值")
	var stats = UnitStats.new()
	var ok: bool = true
	if stats.crit_mark_chance != 0.0:
		print("  ❌ crit_mark_chance 默认值=%f，期望 0.0" % stats.crit_mark_chance); ok = false
	if stats.crit_mark_duration != 5.0:
		print("  ❌ crit_mark_duration 默认值=%f，期望 5.0" % stats.crit_mark_duration); ok = false
	if stats.crit_mark_bonus != 0.50:
		print("  ❌ crit_mark_bonus 默认值=%f，期望 0.50" % stats.crit_mark_bonus); ok = false
	if ok:
		print("  ✅ chance=0.0 duration=5.0 bonus=0.50")
	print("  结果: %s" % ("PASS" if ok else "FAIL"))
	return ok
```

- [ ] **Step 2: 跑测试验证失败**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/crit_mark_smoke.gd"
```
Expected: FAIL — `crit_mark_chance` 字段不存在（`Invalid get index 'crit_mark_chance'`）

- [ ] **Step 3: 加 UnitStats 字段**

`resources/unit_stats.gd`，在 `mark_vuln_bonus` 字段（:235）之后插入：

```gdscript

# ── debuff 型：暴击标注系统（侦查命中概率标注，被标注目标受攻击暴击率提升）──
## 暴击标注触发概率（0.0~1.0，0.30 = 30% 概率标注）
var crit_mark_chance: float = 0.0
## 暴击标注持续时间（秒）
var crit_mark_duration: float = 5.0
## 被标注目标受到攻击时的暴击率加成（0.50 = +50% 暴击率）
var crit_mark_bonus: float = 0.50
```

- [ ] **Step 4: 贯通 UnitStatsTable 读取段**

`resources/unit_stats_table.gd`，在读取段 `"mark_vuln_bonus": stats.mark_vuln_bonus,`（:366）之后插入：

```gdscript
		"crit_mark_chance": stats.crit_mark_chance,
		"crit_mark_duration": stats.crit_mark_duration,
		"crit_mark_bonus": stats.crit_mark_bonus,
```

- [ ] **Step 5: 贯通 UnitStatsTable 写回段**

`resources/unit_stats_table.gd`，在写回段 `stats.mark_vuln_bonus = ...`（:435）之后插入：

```gdscript
	stats.crit_mark_chance = float(result.get("crit_mark_chance", stats.crit_mark_chance))
	stats.crit_mark_duration = float(result.get("crit_mark_duration", stats.crit_mark_duration))
	stats.crit_mark_bonus = float(result.get("crit_mark_bonus", stats.crit_mark_bonus))
```

- [ ] **Step 6: 跑测试验证通过**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/crit_mark_smoke.gd"
```
Expected: PASS — `✅ 全部 PASS`

- [ ] **Step 7: 暂存（commit 需用户授权）**

```bash
git add resources/unit_stats.gd resources/unit_stats_table.gd tests/crit_mark_smoke.gd
```
> ⚠ 询问用户是否 commit。建议消息：`feat(crit-mark): UnitStats 字段层 + table 贯通 + smoke 骨架`

---

## Task 2: registry 映射 + rec_14 改造定义

**Files:**
- Modify: `scripts/systems/modification_registry.gd:574`（`mark_duration` 分支后）
- Modify: `data/modification_modules/recon_mods.gd:162`（`rec_13` 条目后）
- Modify: `tests/crit_mark_smoke.gd`（+test 2、test 3）

- [ ] **Step 1: 在 smoke test 加 test 2、test 3**

`tests/crit_mark_smoke.gd`，在 `_init()` 的 `all_pass = _test_unit_stats_fields() and all_pass` 之后加：

```gdscript
	all_pass = _test_rec14_exists() and all_pass
	all_pass = _test_registry_mapping() and all_pass
```

在文件末尾追加两个测试函数：

```gdscript
## 测试 2：rec_14_crit_designator 存在且 effects 正确
func _test_rec14_exists() -> bool:
	print("\n[测试 2] rec_14_crit_designator 数据")
	var cfg: Dictionary = ReconMods.get_mod_data("rec_14_crit_designator")
	if cfg.is_empty():
		print("  ❌ rec_14_crit_designator 未找到"); print("  结果: FAIL"); return false
	var fx: Dictionary = cfg.get("effects", {})
	var ok: bool = absf(float(fx.get("crit_mark_chance", -1.0)) - 0.30) < 0.001 \
		and absf(float(fx.get("crit_mark_bonus", -1.0)) - 0.50) < 0.001 \
		and absf(float(fx.get("crit_mark_duration", -1.0)) - 5.0) < 0.001
	if ok:
		print("  ✅ effects: chance=%.2f bonus=%.2f duration=%.1f" % [fx.crit_mark_chance, fx.crit_mark_bonus, fx.crit_mark_duration])
	else:
		print("  ❌ effects 不正确: %s" % fx)
	print("  结果: %s" % ("PASS" if ok else "FAIL"))
	return ok

## 测试 3：registry 把 crit_mark_chance effect 映射进结果
func _test_registry_mapping() -> bool:
	print("\n[测试 3] modification_registry crit_mark 映射")
	# apply_with_level(base, mods)：mods 含 level_effects/effects 两种，这里直接喂 effects
	var base: Dictionary = {"crit_mark_chance": 0.0, "crit_mark_bonus": 0.0, "crit_mark_duration": 5.0}
	var mods: Array = [{"effects": {"crit_mark_chance": 0.30, "crit_mark_bonus": 0.50, "crit_mark_duration": 5.0}}]
	var result: Dictionary = ModRegistry.apply_with_level(base, mods)
	var ok: bool = absf(float(result.get("crit_mark_chance", -1.0)) - 0.30) < 0.001 \
		and absf(float(result.get("crit_mark_bonus", -1.0)) - 0.50) < 0.001 \
		and absf(float(result.get("crit_mark_duration", -1.0)) - 5.0) < 0.001
	if ok:
		print("  ✅ 映射后 chance=%.2f bonus=%.2f duration=%.1f" % [result.crit_mark_chance, result.crit_mark_bonus, result.crit_mark_duration])
	else:
		print("  ❌ 映射失败: %s" % result)
	print("  结果: %s" % ("PASS" if ok else "FAIL"))
	return ok
```

- [ ] **Step 2: 跑测试验证失败**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/crit_mark_smoke.gd"
```
Expected: test 2 FAIL（rec_14 未找到）、test 3 FAIL（`crit_mark_chance` effect key 未映射，结果保持 0.0）

- [ ] **Step 3: 加 registry 映射分支**

`scripts/systems/modification_registry.gd`，在 `"mark_duration":` 分支（:573-574）之后插入：

```gdscript
			# debuff 型：暴击标注系统（命中概率标注，被标注目标受攻击暴击率提升）
			"crit_mark_chance":
				if not result.has("crit_mark_chance"):
					result["crit_mark_chance"] = 0.0
				result["crit_mark_chance"] = min(1.0, float(result["crit_mark_chance"]) + float(effect_value))
			"crit_mark_bonus":
				if not result.has("crit_mark_bonus"):
					result["crit_mark_bonus"] = 0.0
				result["crit_mark_bonus"] = float(result["crit_mark_bonus"]) + float(effect_value)
			"crit_mark_duration":
				result["crit_mark_duration"] = maxf(1.0, float(effect_value))
```

- [ ] **Step 4: 加 rec_14 改造定义**

`data/modification_modules/recon_mods.gd`，在 `rec_13_target_designator` 条目结束的 `},`（:162）之后、DATA 字典闭合 `}`（:163）之前插入：

```gdscript

	# debuff 型：暴击指示器（30% 暴击标注 + 暴击率+50%，全队远程优先集火）
	"rec_14_crit_designator" = {
		id = "rec_14_crit_designator",
		name = "暴击指示器",
		name_en = "Crit Designator",
		icon = "res://assets/ui/icons/mod_icons/mod_guidance.png",
		prototype = "精确标定仪",
		description = "攻击命中 30% 概率挂暴击标注，被标注目标受到攻击时暴击率 +50%，持续 5 秒（全队远程优先集火）",
		rarity = "epic",
		power_mult = 1.6,
		cost_research = 320,
		cost_install = 160,
		slot_type = "guidance",
		conflict_group = "crit_designator",
		effects = {crit_mark_chance = 0.30, crit_mark_bonus = 0.50, crit_mark_duration = 5.0},
		unlock_conditions = {required_level = 5}
	},
```

- [ ] **Step 5: 跑测试验证通过**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/crit_mark_smoke.gd"
```
Expected: test 1/2/3 全 PASS

- [ ] **Step 6: 暂存（commit 需用户授权）**

```bash
git add scripts/systems/modification_registry.gd data/modification_modules/recon_mods.gd tests/crit_mark_smoke.gd
```
> ⚠ 建议消息：`feat(crit-mark): registry 映射 + rec_14_crit_designator 改造`

---

## Task 3: 施加链路（module_effect_handler._apply_crit_mark）

**Files:**
- Modify: `scripts/battle/module_effect_handler.gd:91`（`apply_on_hit_side_effects` 内 `_apply_mark` 调用后）、`:493`（`_apply_mark` 方法后）
- Modify: `tests/crit_mark_smoke.gd`（+test 4）

- [ ] **Step 1: 在 smoke test 加 test 4**

`tests/crit_mark_smoke.gd`，在 `_init()` 追加：

```gdscript
	all_pass = _test_apply_crit_mark() and all_pass
```

文件末尾追加（用 Node2D mock target + UnitStats mock；`crit_mark_chance=1.0` 强制触发，避开 randf 随机性）：

```gdscript
## 测试 4：_apply_crit_mark 给目标挂 meta（chance=1.0 强制触发）
func _test_apply_crit_mark() -> bool:
	print("\n[测试 4] _apply_crit_mark 施加 meta")
	var MEH = preload("res://scripts/battle/module_effect_handler.gd")
	var stats = UnitStats.new()
	stats.crit_mark_chance = 1.0   # 强制触发
	stats.crit_mark_duration = 5.0
	stats.crit_mark_bonus = 0.50
	var target = Node2D.new()
	var ok: bool = false
	# chance=1.0 → 必挂 meta
	MEH._apply_crit_mark(target, stats)
	if target.has_meta("_crit_marked_until") and target.has_meta("_crit_mark_bonus"):
		var bonus: float = float(target.get_meta("_crit_mark_bonus", 0.0))
		var expire: float = float(target.get_meta("_crit_marked_until", 0.0))
		var now: float = Time.get_ticks_msec() / 1000.0
		if absf(bonus - 0.50) < 0.001 and expire > now:
			print("  ✅ chance=1.0 → 挂 meta，bonus=0.50，expire>now")
			ok = true
		else:
			print("  ❌ meta 值异常 bonus=%.2f expire=%.2f now=%.2f" % [bonus, expire, now])
	else:
		print("  ❌ 未挂 meta")
	# chance=0.0 → 不挂
	stats.crit_mark_chance = 0.0
	target.remove_meta("_crit_marked_until"); target.remove_meta("_crit_mark_bonus")
	MEH._apply_crit_mark(target, stats)
	if not target.has_meta("_crit_marked_until"):
		print("  ✅ chance=0.0 → 不挂 meta")
	else:
		print("  ❌ chance=0.0 却挂了 meta"); ok = false
	target.free()
	print("  结果: %s" % ("PASS" if ok else "FAIL"))
	return ok
```

- [ ] **Step 2: 跑测试验证失败**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/crit_mark_smoke.gd"
```
Expected: test 4 FAIL — `_apply_crit_mark` 方法不存在（报错或 meta 未挂）

> 若 preload `module_effect_handler.gd` 本身报依赖错误（拉链到未初始化 autoload），记录错误信息，本 test 降级为手动验证（见 Task 7 清单），其余 test 继续。

- [ ] **Step 3: 加 _apply_crit_mark 方法**

`scripts/battle/module_effect_handler.gd`，在 `_apply_mark` 方法（:483-493）之后插入：

```gdscript

# ── debuff 型：暴击标注系统（侦查命中概率标注，被标注目标受攻击暴击率提升）──

## 暴击标注：按 crit_mark_chance 概率给 target 挂暴击标注 meta
## 被标注目标在 bullet.gd 暴击结算时获得 crit_mark_bonus 暴击率加成
## 在 target_selection 索敌时被远程单位优先集火
static func _apply_crit_mark(target: Node, stats: UnitStats) -> void:
	if stats.crit_mark_chance <= 0.0:
		return
	if target == null or not is_instance_valid(target):
		return
	if randf() > stats.crit_mark_chance:
		return
	var expire_at: float = Time.get_ticks_msec() / 1000.0 + stats.crit_mark_duration
	target.set_meta("_crit_marked_until", expire_at)
	target.set_meta("_crit_mark_bonus", stats.crit_mark_bonus)
```

- [ ] **Step 4: 在 apply_on_hit_side_effects 调用**

`scripts/battle/module_effect_handler.gd`，在 `apply_on_hit_side_effects` 内 `_apply_mark(target, stats)`（:91）这一行之后插入：

```gdscript
	_apply_crit_mark(target, stats)                    # 暴击标注（侦查集火眼）
```

- [ ] **Step 5: 跑测试验证通过**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/crit_mark_smoke.gd"
```
Expected: test 1-4 全 PASS

- [ ] **Step 6: 暂存（commit 需用户授权）**

```bash
git add scripts/battle/module_effect_handler.gd tests/crit_mark_smoke.gd
```
> ⚠ 建议消息：`feat(crit-mark): _apply_crit_mark 施加链路`

---

## Task 4: 暴击触发（bullet.gd roll 加成）

**Files:**
- Modify: `scenes/units/bullet.gd:770`（crit_resist 扣减块之后、`randf()` 判定之前）
- Modify: `tests/crit_mark_smoke.gd`（+test 5）

> 说明：bullet.gd 的暴击 roll 在飞行体命中结算时执行，smoke test（无战斗场景）无法端到端驱动。test 5 验证「目标带未过期 `_crit_marked_until` meta 时，加成逻辑的判定条件」用独立函数复刻核心判断，确保逻辑正确；完整端到端验证见 Task 7 手动清单。

- [ ] **Step 1: 在 smoke test 加 test 5（逻辑判定验证）**

`tests/crit_mark_smoke.gd`，在 `_init()` 追加：

```gdscript
	all_pass = _test_crit_mark_bonus_logic() and all_pass
```

文件末尾追加（复刻 bullet.gd 将写入的加成判定：未过期才加 `crit_mark_bonus` 到 `effective_crit`）：

```gdscript
## 测试 5：暴击标注加成判定逻辑（复刻 bullet.gd 的 effective_crit 叠加条件）
func _test_crit_mark_bonus_logic() -> bool:
	print("\n[测试 5] crit_mark 暴击加成判定")
	var target = Node2D.new()
	var ok: bool = true
	# 场景 A：未过期标注 → effective_crit += bonus
	var base_crit: float = 0.10
	var bonus: float = 0.50
	target.set_meta("_crit_marked_until", Time.get_ticks_msec() / 1000.0 + 5.0)
	target.set_meta("_crit_mark_bonus", bonus)
	var eff_a: float = base_crit
	if target.has_meta("_crit_marked_until") and Time.get_ticks_msec() / 1000.0 < float(target.get_meta("_crit_marked_until", 0.0)):
		eff_a += float(target.get_meta("_crit_mark_bonus", 0.0))
	if absf(eff_a - 0.60) > 0.001:
		print("  ❌ 未过期场景: effective=%.2f 期望 0.60" % eff_a); ok = false
	else:
		print("  ✅ 未过期标注: effective_crit 0.10 → %.2f" % eff_a)
	# 场景 B：过期标注 → 不加成
	target.set_meta("_crit_marked_until", Time.get_ticks_msec() / 1000.0 - 1.0)
	var eff_b: float = base_crit
	if target.has_meta("_crit_marked_until") and Time.get_ticks_msec() / 1000.0 < float(target.get_meta("_crit_marked_until", 0.0)):
		eff_b += float(target.get_meta("_crit_mark_bonus", 0.0))
	if absf(eff_b - 0.10) > 0.001:
		print("  ❌ 过期场景: effective=%.2f 期望 0.10" % eff_b); ok = false
	else:
		print("  ✅ 过期标注: effective_crit 保持 0.10")
	# 场景 C：无标注 → 不加成
	target.remove_meta("_crit_marked_until"); target.remove_meta("_crit_mark_bonus")
	var eff_c: float = base_crit
	if target.has_meta("_crit_marked_until") and Time.get_ticks_msec() / 1000.0 < float(target.get_meta("_crit_marked_until", 0.0)):
		eff_c += float(target.get_meta("_crit_mark_bonus", 0.0))
	if absf(eff_c - 0.10) > 0.001:
		print("  ❌ 无标注场景: effective=%.2f 期望 0.10" % eff_c); ok = false
	else:
		print("  ✅ 无标注: effective_crit 保持 0.10")
	target.free()
	print("  结果: %s" % ("PASS" if ok else "FAIL"))
	return ok
```

- [ ] **Step 2: 跑测试验证通过（逻辑层先行，应直接 PASS）**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/crit_mark_smoke.gd"
```
Expected: test 1-5 PASS（逻辑复刻独立于 bullet.gd）

- [ ] **Step 3: 在 bullet.gd 接入暴击加成**

`scenes/units/bullet.gd`，在 crit_resist 扣减块（:767-770）结束之后、`if effective_crit > 0.0 and randf() < effective_crit:`（:771）之前插入：

```gdscript
	# v8.x: 暴击标注——被标注目标受到攻击时暴击率额外提升（独立乘区，不受 crit_resist 扣减）
	if primary != null and primary.has_meta("_crit_marked_until"):
		var _cm_expire: float = float(primary.get_meta("_crit_marked_until", 0.0))
		if Time.get_ticks_msec() / 1000.0 < _cm_expire:
			effective_crit += float(primary.get_meta("_crit_mark_bonus", 0.0))
```

- [ ] **Step 4: 语法校验**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --check-only
```
Expected: 无解析错误（耗时 >70s 正常；忽略引擎 `string_name.cpp` 清理警告）

- [ ] **Step 5: 暂存（commit 需用户授权）**

```bash
git add scenes/units/bullet.gd tests/crit_mark_smoke.gd
```
> ⚠ 建议消息：`feat(crit-mark): bullet.gd 暴击 roll 接入 crit_mark 加成`

---

## Task 5: 过期清理（三处 take_damage 惰性清理）

**Files:**
- Modify: `scenes/units/construct_unit.gd:1178`（`_marked_until` 清理之后）
- Modify: `scenes/units/enemy_unit.gd:1266`（`_marked_until` 清理之后）
- Modify: `scenes/units/swarm_enemy_slot.gd:242`（`_marked_until` 清理之后）

> 说明：暴击加成在 `bullet.gd` 按时间戳判定（过期自然失效），meta 清理沿用 `_marked_until` 的惰性清理范式（命中时顺带 remove），保持三处 take_damage 口径一致。无需新测试（清理是惰性的，test 4/5 已覆盖判定逻辑）。

- [ ] **Step 1: construct_unit.gd 加清理**

`scenes/units/construct_unit.gd`，在 `_marked_until` 过期清理块（:1175-1178 的 `remove_meta("_marked_until")` / `remove_meta("_mark_vuln_bonus")`）之后插入：

```gdscript
			# v8.x: 暴击标注惰性清理（加成按时间戳在 bullet.gd 判定，过期 meta 顺带清掉）
			if has_meta("_crit_marked_until"):
				var _cm_expire: float = float(get_meta("_crit_marked_until", 0.0))
				if Time.get_ticks_msec() / 1000.0 >= _cm_expire:
					remove_meta("_crit_marked_until")
					remove_meta("_crit_mark_bonus")
```

- [ ] **Step 2: enemy_unit.gd 加清理**

`scenes/units/enemy_unit.gd`，在 `_marked_until` 过期清理块（:1264-1266 的 `remove_meta("_marked_until")` / `remove_meta("_mark_vuln_bonus")`）之后插入相同代码块：

```gdscript
			# v8.x: 暴击标注惰性清理
			if has_meta("_crit_marked_until"):
				var _cm_expire: float = float(get_meta("_crit_marked_until", 0.0))
				if Time.get_ticks_msec() / 1000.0 >= _cm_expire:
					remove_meta("_crit_marked_until")
					remove_meta("_crit_mark_bonus")
```

- [ ] **Step 3: swarm_enemy_slot.gd 加清理**

`scenes/units/swarm_enemy_slot.gd`，在 `_marked_until` 过期清理块（:240-242 的 `remove_meta("_marked_until")` / `remove_meta("_mark_vuln_bonus")`）之后插入相同代码块：

```gdscript
			# v8.x: 暴击标注惰性清理
			if has_meta("_crit_marked_until"):
				var _cm_expire: float = float(get_meta("_crit_marked_until", 0.0))
				if Time.get_ticks_msec() / 1000.0 >= _cm_expire:
					remove_meta("_crit_marked_until")
					remove_meta("_crit_mark_bonus")
```

- [ ] **Step 4: 语法校验**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --check-only
```
Expected: 无解析错误

- [ ] **Step 5: 暂存（commit 需用户授权）**

```bash
git add scenes/units/construct_unit.gd scenes/units/enemy_unit.gd scenes/units/swarm_enemy_slot.gd
```
> ⚠ 建议消息：`feat(crit-mark): take_damage 惰性清理 crit_mark meta（三处）`

---

## Task 6: 索敌优先（target_selection 远程集火被标注目标）

**Files:**
- Modify: `scripts/battle/target_selection.gd:5`（顶部加 const）、`:109`（`select_target` 分发前）、文件末尾（加 `_prioritize_crit_marked`）
- Modify: `tests/crit_mark_smoke.gd`（+test 6）

- [ ] **Step 1: 在 smoke test 加 test 6**

`tests/crit_mark_smoke.gd`，在 `_init()` 追加：

```gdscript
	all_pass = _test_prioritize_crit_marked() and all_pass
```

文件末尾追加（构造 mock attacker + 3 个 target：1 个带未过期 crit_mark、2 个无；远程 attacker 应过滤出唯一的被标注目标）：

```gdscript
## 测试 6：_prioritize_crit_marked 远程优先选被标注目标
func _test_prioritize_crit_marked() -> bool:
	print("\n[测试 6] _prioritize_crit_marked 索敌优先")
	var TS = preload("res://scripts/battle/target_selection.gd")
	# mock attacker：远程（attack_range=300 >= 250）
	var attacker = Node2D.new()
	var stats = UnitStats.new()
	stats.attack_range = 300.0
	attacker.set("stats", stats)
	# 3 个 target：t1 带未过期 crit_mark，t2/t3 无
	var t1 = Node2D.new(); t1.set_meta("_crit_marked_until", Time.get_ticks_msec() / 1000.0 + 5.0)
	var t2 = Node2D.new()
	var t3 = Node2D.new()
	var enemies: Array = [t1, t2, t3]
	var ok: bool = true
	# 远程 attacker → 仅返回被标注的 t1
	var filtered_remote = TS._prioritize_crit_marked(attacker, enemies)
	if filtered_remote.size() == 1 and filtered_remote[0] == t1:
		print("  ✅ 远程(attack_range=300): 过滤出唯一被标注目标")
	else:
		print("  ❌ 远程过滤异常 size=%d" % filtered_remote.size()); ok = false
	# 近战 attacker（attack_range=100 < 250）→ 原样返回全部
	stats.attack_range = 100.0
	var filtered_melee = TS._prioritize_crit_marked(attacker, enemies)
	if filtered_melee.size() == 3:
		print("  ✅ 近战(attack_range=100): 原样返回 3 个")
	else:
		print("  ❌ 近战过滤异常 size=%d" % filtered_melee.size()); ok = false
	# 远程但无任何标注 → 原样返回
	stats.attack_range = 300.0
	t1.remove_meta("_crit_marked_until")
	var filtered_none = TS._prioritize_crit_marked(attacker, enemies)
	if filtered_none.size() == 3:
		print("  ✅ 远程但无标注: 原样返回 3 个")
	else:
		print("  ❌ 无标注过滤异常 size=%d" % filtered_none.size()); ok = false
	attacker.free(); t1.free(); t2.free(); t3.free()
	print("  结果: %s" % ("PASS" if ok else "FAIL"))
	return ok
```

- [ ] **Step 2: 跑测试验证失败**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/crit_mark_smoke.gd"
```
Expected: test 6 FAIL — `_prioritize_crit_marked` 不存在

- [ ] **Step 3: 加 REMOTE_RANGE_THRESHOLD 常量**

`scripts/battle/target_selection.gd`，在 `enum TargetMode { DIRECT, INDIRECT, AERIAL }`（:5）之后插入：

```gdscript

## v8.x: 远程单位阈值——attack_range >= 此值视为远程，参与暴击标注集火优先
const REMOTE_RANGE_THRESHOLD: float = 250.0
```

- [ ] **Step 4: 在 select_target 分发前接入过滤**

`scripts/battle/target_selection.gd`，把 `select_target`（:109-114）改为在 match 前先过滤：

```gdscript
## 根据武器类型选目标
static func select_target(attacker: Node2D, enemies: Array, weapon_type: int) -> Node2D:
	# v8.x: 远程单位优先集火暴击标注目标（无标注或非远程则原样）
	enemies = _prioritize_crit_marked(attacker, enemies)
	match weapon_type:
		0: return select_target_direct(attacker, enemies)      # DIRECT
		1: return select_target_indirect(attacker, enemies)     # INDIRECT
		2: return select_target_aerial(attacker, enemies)      # AERIAL
		_: return select_target_direct(attacker, enemies)
```

- [ ] **Step 5: 加 _prioritize_crit_marked 静态方法**

`scripts/battle/target_selection.gd`，在 `_nearest` 方法（:126 起）之前（或文件末尾 `_get_counter_priority` 之后）插入：

```gdscript

## v8.x: 远程单位（attack_range >= REMOTE_RANGE_THRESHOLD）优先选暴击标注目标
## 候选中存在未过期 _crit_marked_until 的目标时，仅返回这些目标；否则原样返回
## 非远程单位不参与集火优先，原样返回
static func _prioritize_crit_marked(attacker: Node2D, enemies: Array) -> Array:
	var stats = attacker.get("stats") as UnitStats
	if stats == null or stats.attack_range < REMOTE_RANGE_THRESHOLD:
		return enemies
	var now: float = Time.get_ticks_msec() / 1000.0
	var marked: Array = []
	for e in enemies:
		if e == null or not is_instance_valid(e):
			continue
		if e.has_meta("_crit_marked_until") and now < float(e.get_meta("_crit_marked_until", 0.0)):
			marked.append(e)
	return marked if not marked.is_empty() else enemies
```

- [ ] **Step 6: 跑测试验证通过**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/crit_mark_smoke.gd"
```
Expected: test 1-6 全 PASS

- [ ] **Step 7: 语法校验**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --check-only
```
Expected: 无解析错误

- [ ] **Step 8: 暂存（commit 需用户授权）**

```bash
git add scripts/battle/target_selection.gd tests/crit_mark_smoke.gd
```
> ⚠ 建议消息：`feat(crit-mark): target_selection 远程优先集火被标注目标`

---

## Task 7: 全量回归 + 手动端到端验证

**Files:** 无代码改动（验证 only）

- [ ] **Step 1: 全量 smoke test**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/crit_mark_smoke.gd"
```
Expected: `✅ 全部 PASS`（test 1-6）

- [ ] **Step 2: 既有 smoke test 回归（确认未破坏）**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --script "tests/phase_instrument_drop_smoke.gd"
```
Expected: `✅ 全部 PASS`（rec_13 / aa_13 数据层未受影响）

- [ ] **Step 3: 全项目语法校验**

```bash
/d/godot/Godot_v4.5.1-stable/Godot_v4.5.1-stable_win64_console.exe --headless --path "." --check-only
```
Expected: 无解析错误（>70s 正常）

- [ ] **Step 4: 手动端到端验证清单（需启动游戏 UI）**

启动游戏，进入战斗，部署装备 `rec_14_crit_designator` 的 recon 单位（侦察无人机/斥候等），观察：

1. **施加**：recon 攻击命中后，目标身上出现暴击标注（暂无 VFX 则观察行为：该目标被集火）。
2. **索敌优先**：场上多个敌方目标时，我方远程单位（曲射炮兵/空射防空/高 attack_range 狙击）优先攻击被标注目标；近战单位保持原索敌。
3. **暴击触发**：攻击被标注目标时暴击率明显上升（观察暴击伤害数字频率）。
4. **过期**：5 秒后标注失效，远程单位恢复正常索敌，暴击率回落。
5. **独立性**：装备 `rec_13_target_designator`（易伤标注）的目标受 +25% 伤害但不额外暴击；同时装 rec_13 + rec_14 的 recon 打出的目标既易伤又高暴击。
6. **敌方不享受**：敌方单位被 recon 标注后，敌方其他单位不会优先攻击其队友（v1 仅玩家方集火）。

> 若手动验证发现平衡问题（秒杀/不触发），回调参数：`rec_14` 的 `crit_mark_chance` / `crit_mark_bonus` / `crit_mark_duration`，或 `target_selection.gd` 的 `REMOTE_RANGE_THRESHOLD`。

- [ ] **Step 5: 最终暂存（commit 需用户授权）**

```bash
git add -A
```
> ⚠ 汇总 commit（若用户授权一次性提交全部）：`feat(crit-mark): 侦查暴击标注系统（rec_14 + 集火优先 + 暴击触发）`

---

## Self-Review 结论

**Spec 覆盖**：
- §4.1 字段 → Task 1 ✓
- §4.2 meta → Task 3（施加）+ Task 5（清理）✓
- §4.3 registry 映射 → Task 2 ✓
- §4.4 REMOTE_RANGE_THRESHOLD → Task 6（放 TargetSelection const，非 GameConstants，更内聚且 smoke 可测）✓
- §5.A 施加 → Task 3 ✓
- §5.B 索敌优先 → Task 6（改为 select_target 分发前一处过滤，三路统一，比三路头部更简洁）✓
- §5.C 暴击触发 → Task 4 ✓
- §5.D 过期清理 → Task 5 ✓
- §6 rec_14 → Task 2 ✓
- §7 文件清单 9 处 → 全覆盖（construct/enemy/swarm 三处合并描述）✓
- §9 测试 → Task 1-6 smoke + Task 7 手动 ✓

**Placeholder 扫描**：无 TBD/TODO；每步含完整代码或确切命令。

**类型一致**：`_crit_marked_until` / `_crit_mark_bonus` meta 名、`crit_mark_chance` / `crit_mark_duration` / `crit_mark_bonus` 字段名、`_apply_crit_mark` / `_prioritize_crit_marked` 方法名、`REMOTE_RANGE_THRESHOLD` 常量名——全计划一致。`rec_14` effect key（`crit_mark_chance` / `crit_mark_bonus` / `crit_mark_duration`）与 registry 分支、UnitStats 字段完全对齐。

**与 spec 的偏差（已记录）**：
1. `REMOTE_RANGE_THRESHOLD` 放 `TargetSelection` const 而非 `GameConstants`（避免 autoload 依赖，smoke 可测）。
2. 索敌优先在 `select_target:109` 一处过滤（三路统一），非 spec §5.B 描述的「三路头部」。
3. `UnitStats` 字段用 `var`（项目惯例）非 spec 的 `@export`。
4. `crit_mark_priority` 字段移除（YAGNI）——索敌优先纯靠 `is_remote_unit`（`attack_range >= REMOTE_RANGE_THRESHOLD`）判定，spec §4.1 的 4 字段精简为 3 字段。
