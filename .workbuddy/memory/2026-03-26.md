# 2026-03-26 工作日志

## 全面项目检查（不合理设定分析）

完成了对整个项目的全面代码检查，发现以下主要问题：

### P0 严重问题
1. **能量系统无回复**：`energy_manager.gd` 的 `_process()` 是空的，`ENERGY_REGEN_PER_SEC` 和能量回收卡（energy_regen_1/2/3）的每秒回复效果完全未实现
2. **双套数值矛盾**：`default_cards.gd` 的卡牌描述数值（伤害/射程/攻速）与 `unit_stats_table.gd` 的实战数值严重不符（差异达-25%~-53%）
3. **精英/BOSS掉落ID全部无效**：`enemy_archetypes.gd` 中手写掉落如 `smg_mk2`, `bulwark`, `titan_mk2` 等，在游戏中不存在对应蓝图（实际格式是 `bp_ww1_001`）

### P1 中等问题
4. **任务奖励碎片ID无效**：`quest_definitions.gd` 奖励使用了同样不存在的ID
5. **武器部署费用缺失**：`battle_manager.gd` 只扣平台卡费用，不扣武器卡费用
6. **击杀护盾分配给所有单位**：应只给击杀者，实际给了全体玩家单位
7. **平台类型错配**：布雷德利IFV被分配MEDIC类型，谢尔曼被分配RAIDER，虎式被分配GUARD

### P2 轻微问题
8. 冷战BOSS（米格-29）HP低于部分精英且掉落为空
9. 调试代码（_dbg_log写文件）混入生产代码，每次查询都触发文件IO
10. 第一波敌人延迟9-14秒才出现（timer从0开始）
11. 势力声望惩罚叠加（攻克关卡-10 + 任务完成-30 = 单次行动-40）
