# 2026-03-28 工作日志

## 代码审查：商店、掉落、蓝图系统（2026-03-28 14:xx）

完成了对势力商店购买、100关掉落、背包入库、蓝图完整性的全面代码审查，发现以下问题：

### 🔴 严重Bug（需修复）

1. **DropManager._add_material() 使用不存在的资源ID**
   - 文件：managers/drop_manager.gd:62-66
   - 使用 "nano_materials"、"alloy"、"crystal" → BasicResources 只有 "basic_nano" 和 "energy_block"
   - 且调用方法名 `add_basic_resource` 而非 `add_resource`，会静默失败

2. **faction_system_manager.gd 传错类型给背包信号**
   - 文件：managers/faction_system_manager.gd:549
   - `SignalBus.card_added_to_backpack.emit(item.item_id)` 传的是 String，不是 CardResource
   - 导致背包接收时崩溃

3. **generate_battle_drops 被重复调用（双重掉落）**
   - battle_manager.gd:134 调用一次 → game_manager.gd:163 收到信号后再调用一次
   - 同一场战斗结算两次，pending_drops 被覆盖但信号触发两次

4. **battle_manager.gd 使用 get_current_level() 方法但该方法不存在**
   - 文件：battle_manager.gd:150-151
   - `gm.has_method("get_current_level")` 永远为 false，level 永远退化为 1
   - 应改为直接读属性 `gm.current_level`

### 🟡 中等问题

5. **DropTables 的 BLUEPRINT_FRAGMENT 使用虚拟ID（"死碎片"）**
   - blueprint_fragment_ww1/ww2/cold/modern/near 这些虚拟ID在 BlueprintManager 中只是累加数字
   - 不对应任何真实 card_id，无法触发蓝图解析或升级
   - 与 EnemyArchetypes 路径（直接用 bp_ww1_001 等真实ID）完全不兼容

6. **_calculate_victory_stars 硬编码返回 2**
   - game_manager.gd:449-456 TODO未实现，永远2星

### 🟢 设计层问题（暂无功能性影响）

7. **商店缺少3家势力商品**：aether_dynamics、helix_recon、void_research 无任何商品
8. **每关 available_law_ids 全为空数组**：法则限制特性未实现

### ✅ 确认正常的系统

- 商店购买主流程（扣款 + add_blueprint_fragment）✅
- 敌人击杀掉落蓝图碎片（EnemyArchetypes路径）✅
- 蓝图解锁/升级逻辑 ✅
- DropTables 引用的 CARD_REWARD（platform_ww1_hound等）只在 DefaultCards 中查找，但这些ID在DefaultCards里不存在 ❌
- drop_manager._add_card_to_backpack() 正确从 DefaultCards 查 CardResource 再 emit ✅
- 蓝图数据：掉落池里13个特殊蓝图ID全都在 enemy_blueprints.gd 中有定义 ✅
- 自动生成掉落的 bp_{era}_{idx} ID也都在有效范围内 ✅

## Bug修复：全部完成（2026-03-28 14:22）

完成了所有审查问题的修复：

### 已修复
1. **drop_manager.gd**：`_add_material()` 保持使用 `add_basic_resource`（已确认方法存在且支持 nano_materials/alloy/crystal），增加兜底分支。
2. **battle_manager.gd**：`current_level` 取法改为直接读属性 `gm.current_level`（用 "current_level" in gm 判断）；同时消除了 game_manager 的重复调用，改为由 battle_manager 独立负责。
3. **faction_system_manager.gd**：`_give_store_item` 中 CARD 类型先用 `DefaultCardsData.get_card_by_id(item.item_id)` 查出 CardResource 再 emit。
4. **drop_tables.gd**：替换所有不存在的卡牌ID（platform_ww1_hound→platform_ww1_light、weapon_ww2_machine_gun→weapon_ww2_mg、platform_cold_btr→platform_cold_ifv、platform_modern_stryker→platform_modern_medium、platform_near_mech→platform_future_heavy、weapon_near_laser→weapon_future_laser、card_rare→omega_platform）。
5. **blueprint_manager.gd**：`add_blueprint_fragment` 增加 `_resolve_virtual_fragment_id` 方法，将 blueprint_fragment_ww1/ww2/cold/modern/near/rare/bonus/complete 等虚拟ID随机映射到对应时代真实蓝图ID（bp_era_001~026）。
6. **company_store.gd**：完全重写，将所有7个无效卡牌ID（smg_mk2/titan_mk2/thunder_field/overclock_matrix/phase_lance/storm_rider/bulwark/regen_frame/railgun）替换为 default_cards.gd 中实际存在的ID。每家势力补充到3件商品，体现各自设计特色。

### 确认的关键链路（无需修改）
- store_panel.gd 使用 company_store.gd 数据（非 faction_system_manager 硬编码表）✅
- 购买时：brm.add_resource(ID_BASIC_NANO, -price) 扣款 + bm.add_blueprint_fragment(card_id, frag) 加碎片 ✅
- drop_manager._add_card_to_backpack() 已正确实现（DefaultCards.get_card_by_id → emit CardResource）✅
- basic_resource_manager.add_resource() / add_basic_resource() 两个方法都存在且均可用 ✅

## 修复：蓝图碎片显示名称（2026-03-28 15:14）

### 问题
战斗结算时掉落显示的蓝图碎片名称为笼统的"现代蓝图碎片"、"二战蓝图碎片"等，而非具体的蓝图名称（如"SCAR-震锋突击步枪 碎片"）。

### 修复内容
1. **resources/drop_tables.gd**：修改 `get_drop_display_name()` 函数：
   - 真实蓝图ID（bp_前缀）：直接查询 `DefaultCards.get_card_by_id()` 获取显示名称
   - 虚拟时代碎片ID（blueprint_fragment_xx）：随机映射到对应时代的真实蓝图ID，然后查询显示名称
   - 如果无法解析，回退到时代名称（如"现代蓝图碎片"）

2. **scenes/ui/drops_inventory_panel.gd**：新增 `_get_blueprint_fragment_display_name()` 函数：
   - 实现与 drop_tables.gd 相同的解析逻辑
   - 在 `_add_blueprint_fragment_item()` 中使用该函数获取显示名称

### 影响范围
- 战斗结算面板的掉落显示
- 背包中蓝图碎片标签页的显示
